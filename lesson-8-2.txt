a. ответ undefined, т.к. по условию не попадём внутрь, где определяется переменная a
b. ответ undefined, т.к. переменная a не определена
c. не совсем понял, либо это ссылка на функцию a(x) либо на неопределённую переменную
d. Ответ 10, т.к. при помощи arguments[2] = 10 записали в a и вывели a
e. выдаст ObjectWindow, равносильно вызову a(), внутри указатель на объект this